initialize-open-close
deleteComponents
	"This method is called when a Phidget is detached.
	Delete the previously in 'initializeComponents' built component: textLCD.
	It must have empty rows now."
	
	textLCD initializeRowsEmpty.