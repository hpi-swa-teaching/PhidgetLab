accessing
setCharAt: index with: aPhdCrCustomChar
	"Method that is called from the CustomCharPicker when character is picked. Needs a dummie index because picker returns 2 arguments."

	upper := aPhdCrCustomChar upper.
	lower := aPhdCrCustomChar lower.